pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: npx biome check --write --no-errors-on-unmatched {staged_files}
      glob: "*.{js,jsx,ts,tsx,json}"

pre-push:
  jobs:
    - name: ci
      run: npm run ci
